{# templates/news/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Create News{% endblock %}

{% block body %}
    <h1>Create new News</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Save</button>
    {{ form_end(form) }}
    
    <a href="{{ path('news_index') }}">Back to list</a>
{% endblock %}
